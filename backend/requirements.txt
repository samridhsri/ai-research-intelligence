# Web & API
fastapi==0.110.0
uvicorn==0.28.0
python-multipart==0.0.9

# DB & Vector DB
psycopg2-binary==2.9.9
pgvector==0.3.6
pinecone-client==3.1.0

# Object Storage (Supabase)
supabase==2.4.0

# arXiv & PDF ingestion
arxiv==2.1.3
pymupdf==1.23.26

# Embeddings — local default; swap for openai later via EMBEDDING_PROVIDER env var
sentence-transformers==3.3.1

# OpenAI (optional — only needed when EMBEDDING_PROVIDER=openai)
openai==1.57.4

# LangGraph (installed now, used in later sessions)
langgraph==0.2.70
langchain-core==0.3.29
langchain-text-splitters>=0.3.0

# Redis & RQ Task Queue
redis==5.0.3
rq==1.16.1

# Cohere Rerank API
cohere==5.5.0

# Sentry Observability
sentry-sdk[fastapi]==2.4.0

# Pytest boundary verification
pytest==8.1.1
pytest-asyncio==0.23.6

# Utilities
python-dotenv==1.0.1
tqdm==4.67.1


